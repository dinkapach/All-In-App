import { ClubManually } from './clubManually.model';
import { Club } from './club.model';

export class Receipt {
    isManual : boolean;
    clubId?: any;
    //clubManuallyId?: any;
    img : string;
}