// import { ClubManually } from './clubManually.model';
// import { Club } from './club.model';

export class Manager {
    id: number;
    userName: string;
    firstName: string;
    lastName: string;
    password: string;
    email: string;
    clubId: string;
    permissions: string;
}